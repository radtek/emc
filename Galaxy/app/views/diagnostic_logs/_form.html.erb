<%= form_for(@diagnostic_log) do |f| %>
  <% if @diagnostic_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diagnostic_log.errors.count, "error") %> prohibited this diagnostic_log from being saved:</h2>

      <ul>
      <% @diagnostic_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :create_time %><br>
    <%= f.datetime_select :create_time %>
  </div>
  <div class="field">
    <%= f.label :component %><br>
    <%= f.text_field :component %>
  </div>
  <div class="field">
    <%= f.label :log_type %><br>
    <%= f.number_field :log_type %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
